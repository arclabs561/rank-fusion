[package]
name = "rank-fusion"
version = "0.1.5"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Fast rank fusion for hybrid search — RRF, CombMNZ, Borda, Weighted"
keywords = ["search", "ranking", "fusion", "rrf", "hybrid"]
categories = ["algorithms", "data-structures"]
repository = "https://github.com/arclabs561/rank-fusion"
readme = "README.md"
authors = ["Arc <attobop@gmail.com>"]

[dependencies]
# Zero dependencies

[dev-dependencies]
criterion = "0.5"
proptest = "1"

[[bench]]
name = "fusion"
harness = false
