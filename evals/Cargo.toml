[package]
name = "rank-fusion-evals"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rank-fusion = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

